# L2 — Three Hearts / Orchestration

## Current Rating: 0 (no code)

## Spec Rating (STATE.yaml): 20

## Delta: −20 (spec was aspirational).

## Shipped Components

NONE — no orchestrator package under `src/ven0m/`.

## Spec Claims

All hearts/dispatch claims are aspirational. No sequential dispatcher, planner graph, or phase engine exists in-repo.

## Recommendation

Treat STATE ratings for this layer as **documentation targets only** until first dispatcher module lands; keep `gap` at `100` while rating stays `0`.
